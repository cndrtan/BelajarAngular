export class Contact{
    //constructor(private name:string, private email:string, private phone:string) {}
    id?: number;
    name!: string | null;
    email!: string | null;
    phone!: string | null
}